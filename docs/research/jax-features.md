# JAX and Flax features for Dew

Research date: 2026-09-06. Dew source: `65fd2f4`.

This extends [google-jax-stack.md](google-jax-stack.md). The [MaxText inventory](maxtext-parity.md) names the missing training capabilities. This note identifies the upstream primitives that can implement them. Fit entries are recommendations, not approved designs.

## Versions and evidence

The requested command printed JAX `0.11.1` and Flax `0.12.9`:

```sh
/home/mrwhite0racle/Desktop/dew/.venv/bin/python -c 'import jax, flax; print(jax.__version__, flax.__version__)'
```

Installed distribution metadata also reports jaxlib `0.11.1`, Optax `0.2.8`, Grain `0.2.18`, Orbax Checkpoint `0.12.4` and Qwix `0.1.8`. JAX source links below use tag `jax-v0.11.1`; Flax links use `v0.12.9`. MaxText links use the 0.2.4 commit `538fe7a3f3376d94cf3f04e77741aa6d7e8efa45`. Its base file matches the release tag byte for byte.

“API verified” means the named API or source implementation exists in that installed release. It does not mean a TPU/GPU kernel ran. “Older floor not established” means no minimum historical release was proven. These features require no version upgrade in the current environment unless a row says otherwise. Dew's package metadata still advertises `jax>=0.4.28` and `flax>=0.8.4` (`pyproject.toml:14-23`); those old floors are not evidence that the current source or these additions work on them.

Tokamax `0.0.13` was installed without dependencies in `/tmp/dew-research-kernels` for source reading. Its metadata requires Python `>=3.12` and jax/jaxlib `>=0.11.0` ([release](https://pypi.org/project/tokamax/0.0.13/); wheel `METADATA:209-218`). The release wheel is the version authority for its file:line citations. GitHub `main` links locate the corresponding upstream modules and may move. No tagged tokamax release was listed by the official tags API. The wheel's public exports do not include the fused GDN function named in the earlier main-branch survey (`tokamax/__init__.py:15-41`). This note does not claim it as an available 0.0.13 feature.

No cloud run, model download or GPU/TPU benchmark was performed. Expected wins are hypotheses unless the evidence column names a recorded measurement. The new experiment checked XLA flag parsing on the CPU backend only. Existing Dew measurements are cited with their original hardware and scope.

## Already present and easy to misclassify

Dew already uses state donation, `jax.set_mesh`, `jax.shard_map` for pipeline transfers, Linen layer scans, async Orbax saves, OCDBT, partial restore and Grain first-fit packing. These rows identify limits or additional uses. They are not missing dependencies. `jax.sharding.use_mesh` is absent from the installed JAX release. The current public context API is `jax.set_mesh`.

## Activation memory

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| `jax.checkpoint_policies.everything_saveable` | Allow autodiff to retain every residual. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | A useful baseline; does not reduce HBM. | Unmeasured. |
| `jax.checkpoint_policies.nothing_saveable` | Recompute internal residuals from checkpoint inputs. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Lower residual HBM at the cost of repeated compute. | Unmeasured. |
| `jax.checkpoint_policies.dots_saveable / checkpoint_dots` | Retain dot products, including batched dots. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Can retain the quadratic attention score matrix. | Unmeasured. |
| `jax.checkpoint_policies.dots_with_no_batch_dims_saveable / checkpoint_dots_with_no_batch_dims` | Retain dot products without batch dimensions. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Already used by DiT; useful for decoder block remat after tracing actual dot dimensions. | Unmeasured. |
| `jax.checkpoint_policies.save_only_these_names` | Retain only values marked with an allowed checkpoint name. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Select expensive projections without retaining all intermediates. | Unmeasured. |
| `jax.checkpoint_policies.save_any_names_but_these` | Retain named values except the excluded names. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Applies to named values only; unnamed values recompute. | Unmeasured. |
| `jax.checkpoint_policies.save_from_both_policies` | Combine boolean save policies with logical OR. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Do not assume this composes the three-way Offloadable result correctly. | Unmeasured. |
| `jax.checkpoint_policies.save_anything_except_these_names` | Exported exclusion helper currently returns an always-true lambda. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder policy; the DiT path uses only one selector. | Trainer execution capability applied through Linen remat. | Do not adopt. In this installed release it does not implement its apparent exclusion contract. | Unmeasured. |
| Named residuals and `save_dot_except_mlp` | Mark array values with checkpoint_name and retain selected projection names. | [jax/_src/ad_checkpoint.py:893-930](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L893-L930); [MaxText policy recipes](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/nnx_decoders.py#L1189-L1225) | JAX 0.11.1 API verified; older floor not established | Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder checkpoint names. save_dot_except_mlp is a MaxText recipe, not a JAX API. | Trainer remat selection; named values inside attention and MLP modules. | Likely lower recompute cost than full remat. Exact retained memory depends on the traced graph. | Unmeasured. |
| `save_and_offload_only_these_names` | Save selected names on device and move a disjoint set to a second memory space. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218); [JAX memory spaces](https://docs.jax.dev/en/latest/201/memory-spaces.html) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No decoder host-offload policy. | Trainer execution capability; checkpoint names in decoder layers. | Lower device residual memory. Requires pinned host capacity and bidirectional transfer bandwidth. The two name sets must be disjoint. | Unmeasured. |
| `offload_dot_with_no_batch_dims` | Offload residuals from eligible non-batched dots. | [jax/_src/ad_checkpoint.py:62-218](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/ad_checkpoint.py#L62-L218) | JAX 0.11.1 API verified; older floor not established | DiT dot/full-remat helper (`src/dew/nn/dit.py:263-284`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No offloaded dot policy. | Trainer remat capability. | May offload many large projections. Trace transfer volume before using a broad selector. | Unmeasured. |
| Activation offload with `pinned_host` | Use checkpoint offload_src=device and offload_dst=pinned_host. | [JAX host-offload guide](https://docs.jax.dev/en/latest/201/memory-spaces.html); [MaxText offload implementation](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/nnx_decoders.py#L1226-L1262) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | Layout has no memory-kind field and decoder has no activation-offload names. | Trainer placement/remat capabilities. | Capacity benefit is plausible; PCIe or host-memory traffic can dominate. JAX guide examples are illustrative, not a Dew measurement. | Unmeasured. |

## State residency

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| Optimizer-state offload | Keep moments in pinned_host between steps and transfer them around the update. | [JAX optimizer offload](https://docs.jax.dev/en/latest/201/memory-spaces.html#offloading-optimizer-state); [MaxText optimizer transfers](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/trainers/pre_train/train.py#L562-L632) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). One state input/output placement (`src/dew/training/trainer.py:392-408`). | Moments, parameters and EMA inherit device-memory placements. | Trainer state-placement capability and Layout memory kinds. | Frees steady-state optimizer HBM; whole-state transfers and update-time peak can erase the gain. | Unmeasured. |
| Layer-wise parameter offload | Store weights on host and fetch each scanned layer before use. | [JAX parameter offload](https://docs.jax.dev/en/latest/201/memory-spaces.html#offloading-parameters); [MaxText parameter device placement](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/trainers/pre_train/train.py#L491-L506) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No host-resident weights or layer fetch schedule. | Trainer placement plus decoder scan execution. | JAX guide requires scanning axis 0 for the demonstrated benefit. Python-unrolled layers can keep all weights live; MaxText moving parameters to device is not proof of per-layer residency. | Unmeasured. |
| Separate optimizer layout / ZeRO-1 | Partition moments over replica devices without applying that placement to model weights. | [MaxText optimizer sharding](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/base.yml#L689-L690); [JAX sharding](https://docs.jax.dev/en/latest/201/sharding.html) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). | Dew applies the same parameter-derived rule mechanism to the whole train state and refuses data-axis parameter rules. | Trainer state-placement capability; distinct parameter and optimizer placements. | Could reduce replicated optimizer HBM. Introduces update collectives and changes checkpoint placement requirements. | Unmeasured. |

## Attention and device kernels

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| `jax.nn.dot_product_attention` backend selection | Use XLA or cuDNN through the public attention API. | [JAX attention API](https://docs.jax.dev/en/latest/_autosummary/jax.nn.dot_product_attention.html); [jax/_src/nn/functions.py:1086-1287](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/nn/functions.py#L1086-L1287) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | Already used. Public default None selects XLA; Dew auto adds its own cuDNN selection. | Keep attention_impl as the single dispatch seam. | No new feature win from replacing the same call. Public Q/K/V dtypes must match; KV heads must divide Q heads; value geometry matches key geometry. | Existing single-RTX-4080 evidence in docs/performance.md:142-166. No new run. |
| Length metadata and logsumexp residuals | Pass per-example query/KV lengths and request attention logsumexp. | [JAX attention arguments](https://docs.jax.dev/en/latest/_autosummary/jax.nn.dot_product_attention.html) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). KV length metadata for odd-length padding (`src/dew/nn/attention.py:334-353`). | Dew exposes masks/windows but not general length arrays or returned residuals. | attention_impl and Rollout metadata. | Avoid padding work when a fused backend supports it. Logsumexp can support exact block-result combination; it is not a paging scheduler. | Unmeasured. |
| Hopper cuDNN head dimensions | Support bf16/fp16 head dimensions through 256 on Hopper or newer. | [jax/_src/cudnn/fused_attention_stablehlo.py:354-402](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/cudnn/fused_attention_stablehlo.py#L354-L402); [Public backend constraints](https://docs.jax.dev/en/latest/_autosummary/jax.nn.dot_product_attention.html) | JAX 0.11.1; Hopper+ for width 256; compatible cuDNN | Dew cuDNN limit is 128 (`src/dew/nn/attention.py:441-453,618-637`). | Dew excludes supported 256-wide heads on Hopper. | attention_impl device capability selection. | Potential fused path for wide heads. Ada remains capped at 128. Odd-length training restriction in this JAX release is conditional on bias, not all odd sequences. | Unmeasured. |
| Pallas Mosaic GPU flash attention | Program tiled attention with asynchronous GPU memory operations and MMA. | [jax/experimental/pallas/ops/gpu/attention_mgpu.py:15-35](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/gpu/attention_mgpu.py#L15-L35); [JAX Mosaic GPU guide](https://docs.jax.dev/en/latest/pallas/gpu/index.html) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | Dew has no Mosaic GPU attention path. | attention_impl; prefer an upstream maintained wrapper before a custom kernel. | A potential fused path for unsupported attention shapes. Kernel-specific hardware, dtype, gradient and mask support must be checked; the existence of an example is not a production guarantee. | Unmeasured. |
| Pallas Triton backend | Lower per-device Pallas kernels through Triton. | [jax/_src/pallas/triton/core.py:13-26](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/pallas/triton/core.py#L13-L26); [jax/_src/pallas/triton/gpu_info.py:27-49](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/pallas/triton/gpu_info.py#L27-L49) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | No direct Triton path in Dew. The installed JAX GPU-info enum omits RTX 4080. | attention_impl or MoE/GDN kernel seam. | Useful on an explicitly supported device. Do not bypass a GPU whitelist to claim support. | Unmeasured here. google-jax-stack.md records the older RTX 4080 refusal; source still omits that model. |
| tokamax GPU attention | Dispatch attention to XLA, chunked XLA, Triton, cuDNN or Mosaic. | [tokamax 0.0.13](https://pypi.org/project/tokamax/0.0.13/); [attention API; release wheel lines 81-110](https://github.com/openxla/tokamax/blob/main/tokamax/_src/ops/attention/api.py#L81-L110) | tokamax 0.0.13 requires Python >=3.12 and jax/jaxlib >=0.11.0 | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | Only MoE imports tokamax; attention does not. | attention_impl with capability checking and serialized tuning choices. | Supports softcap in its extended API. Mosaic implementation accepts fp32/fp16/bf16, requires sm90/sm100, and rejects paging/dropout in this wheel (_src/ops/attention/pallas_mosaic_gpu.py:48-126). | Unmeasured. |
| tokamax ragged dot backend/tile selection | Run expert grouped matmuls through device-specific kernels and tuning configurations. | [tokamax release](https://pypi.org/project/tokamax/0.0.13/); [MaxText grouped-matmul dispatch](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/moe.py#L1519-L1545) | tokamax 0.0.13 requires Python >=3.12 and jax/jaxlib >=0.11.0 | Dropless sorted routing and XLA/tokamax grouped matmul (`src/dew/nn/moe.py:236-443`). Mixture fields (`src/dew/nn/backbones/causal_transformer.py:192-206`). | ExpertLinear has an optional tokamax call; the decoder Mixture value does not expose it. No pinned tuning config. | MoE kernel capability at the decoder execution boundary. | Potential expert GEMM improvement; sorting, transfers and weight gathering can still dominate. | Unmeasured. |
| Splash TPU masks and kernels | Tile attention over structured masks with configurable forward/backward kernels. | [jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py:488-548](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py#L488-L548); [MaxText Splash construction](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/attention_op.py#L1745-L1808) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | Dew calls the older flash_attention function and materializes explicit bias for masks/windows. | attention_impl; retain structured masks until the kernel boundary. | Potentially lower HBM and less masked work for long contexts. Backward support and head/batch vmap placement require TPU verification. | Unmeasured. |
| Splash forward block configuration | Tune block_q, block_kv and block_kv_compute. | [jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py:499-520](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py#L499-L520); [MaxText global/local blocks](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/base.yml#L1155-L1191) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | No Splash block configuration. | attention_impl typed per-device settings. | Tile geometry changes VMEM use and occupancy. MaxText base uses 512; installed JAX defaults use 128. Neither default is a Dew benchmark. | Unmeasured. |
| Splash backward blocks and layouts | Tune dKV/dQ blocks, fused backward and Q/K/V tile layouts. | [jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py:503-548](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py#L503-L548) | JAX 0.11.1 API verified; older floor not established | Reference, XLA, cuDNN and TPU flash dispatch (`src/dew/nn/attention.py:484-662`). | No separate forward/backward tuning. | attention_impl kernel configuration. | Fused backward forbids separate dQ block sizes. Forward-only settings do not establish trainability; require has_backward_blocks and shape checks. | Unmeasured. |
| Ragged paged attention | Consume packed queries, page tables, cumulative query lengths and per-request KV lengths. | [jax/experimental/pallas/ops/tpu/ragged_paged_attention/kernel.py:732-776](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/tpu/ragged_paged_attention/kernel.py#L732-L776); [MaxText vLLM RPA adapter](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/attentions.py#L1118-L1180) | JAX 0.11.1 API verified; older floor not established | Fixed compute-dtype KV cache (`src/dew/nn/attention.py:262-313`). | No page allocator, block tables, scheduler or write-to-cache contract. | Rollout cache boundary and attention_impl. | TPU kernel supports mixed prefill/decode, softcap, window and cache scales. It reads an existing cache; standalone kernel source leaves cache writing to its caller. Metadata is int32 and Q heads must divide into KV groups. | Unmeasured. |
| Collective Pallas matmul | Overlap a ring all-gather with tiled matrix multiplication. | [JAX collective-matmul tutorial](https://docs.jax.dev/en/latest/pallas/gpu/collective_matmul.html); [jax/experimental/pallas/ops/gpu/collective_matmul_mgpu.py:15-35](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/gpu/collective_matmul_mgpu.py#L15-L35) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). Dropless sorted routing and XLA/tokamax grouped matmul (`src/dew/nn/moe.py:236-443`). | No explicit collective matmul kernel. | Per-device module kernel via shard_map and MeshSpec/Layout. | Potential overlap on fast GPU links. Buffer lifetime, synchronization and backward collectives are part of the implementation. Distinct from XLA windowed-einsum flags. | Unmeasured. |

## Sharding and compiled execution

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| `jax.shard_map` | Write a per-device function with explicit collective communication. | [JAX shard_map](https://docs.jax.dev/en/latest/_autosummary/jax.shard_map.html); [MaxText per-device MoE](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/moe.py#L2306-L2360) | JAX 0.11.1 API verified; older floor not established | Already used for stage ppermute (`src/dew/nn/backbones/causal_transformer.py:1569-1581`). | MoE and attention use global arrays; no per-device overlap path. | Keep Auto global training; use shard_map only at kernel/collective seams. | Enables ring attention and expert dispatch with controlled communication. Incorrect replication assertions or collective order can corrupt results or deadlock. | Unmeasured. |
| Explicit sharding in types | Use AxisType.Explicit and inspect shardings through jax.typeof. | [JAX sharding](https://docs.jax.dev/en/latest/201/sharding.html); [MaxText shard_mode](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/base.yml#L529-L531) | JAX 0.11.1 API verified; older floor not established | Six-axis Auto mesh (`src/dew/training/distributed.py:75-154`). Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). | Dew forces Auto axes. Parameter rules are not a complete explicit activation program. | MeshSpec/Layout as an opt-in experiment across the full model/objective. | Earlier ambiguity errors and clearer layout intent. Every operation with ambiguous output sharding needs an explicit choice; not a free speed improvement. | Unmeasured. |
| Mesh context API | Set the concrete mesh with jax.set_mesh; query its abstract mesh while tracing. | [JAX mesh context](https://docs.jax.dev/en/latest/parallel.html); [jax/_src/sharding_impls.py:1-35](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/sharding_impls.py#L1-L35) | JAX 0.11.1 API verified; older floor not established | Already enters jax.set_mesh for compile and dispatch (`src/dew/training/trainer.py:409-414`). | The requested jax.sharding.use_mesh name is absent from installed 0.11.1. Dew already uses the current context API. | No migration needed. Explicit mode is the missing experiment, not a renamed mesh context. | Calling under a different context can trigger a second trace. Keep compile and dispatch contexts identical. | Performance unmeasured. API inspection observed use_mesh=False and set_mesh=True. |
| `lax.scan` / Linen scan unroll | Unroll several iterations inside one compiled scan body. | [JAX scan](https://docs.jax.dev/en/latest/_autosummary/jax.lax.scan.html); [Linen scan unroll](https://github.com/google/flax/blob/v0.12.9/flax/linen/transforms.py#L1153-L1164) | JAX 0.11.1 / Flax 0.12.9 APIs verified; older floor not established | Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). Pipeline iteration scan (`src/dew/nn/backbones/causal_transformer.py:1615-1617`). | Dew does not expose unroll; scans use the default one-iteration body. | Decoder execution capability; per-kernel tuning for GDN/ring loops. | Trade compile size for loop overhead and scheduling freedom. Existing stack/unstack copies can dominate small models. | No unroll sweep. Existing scan/plain timings: docs/concepts/distributed.md:109-118. |
| Linen `remat_scan` | Nest scanned/rematerialized loops over factored depth. | [Flax remat_scan](https://github.com/google/flax/blob/v0.12.9/flax/linen/transforms.py#L1087-L1149) | Flax 0.12.9 API verified; no NNX required | Consecutive equal-spec layer scan (`src/dew/nn/backbones/causal_transformer.py:524-558`). | No nested remat_scan. Current stored tree is deliberately unstacked. | Trainer execution capability applied to decoder homogeneous groups. | Potential sublinear activation memory with depth. Transforming a model directly changes its stacked parameter representation; preserve or explicitly migrate the external tree. | Unmeasured. |
| `donate_argnames` | Donate a JIT argument by its Python name. | [JAX jit donation](https://docs.jax.dev/en/latest/_autosummary/jax.jit.html) | JAX 0.11.1 API verified; older floor not established | Already donates the state with donate_argnums=(0,) (`src/dew/training/trainer.py:401-407`). | No capability gap. Changing syntax does not add donation. | Retain the existing Trainer donation contract. | No expected memory win over current donation. A donated argument must not be reused. | Incremental benefit unmeasured. Existing alias audit: docs/performance.md:111. |
| Physical array layouts | Use jax.experimental.layout.Layout, Format and with_layout_constraint. | [jax/experimental/layout.py:15-21](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/layout.py#L15-L21); [jax/_src/layout.py:33-148](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/layout.py#L33-L148) | JAX 0.11.1 API verified; older floor not established | Parameter Layout rules and tolerance (`src/dew/training/distributed.py:205-305`). JIT sharding boundary (`src/dew/training/trainer.py:401-408`). | Dew Layout describes device partitioning, not physical memory layout or tiling. | attention_impl/kernel I/O and Trainer compile boundary; avoid conflating the two Layout meanings. | May remove transposes/copies at kernel boundaries. A concrete Format layout requires a concrete sharding; API and backend support are experimental. | Unmeasured. |
| Auto PGLE | Profile instruction latencies and recompile using measured scheduling costs. | [JAX PGLE guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html#auto-pgle) | JAX 0.11.1 API verified; older floor not established | Returns the jitted function after FLOP inspection (`src/dew/training/trainer.py:364-416`). | No PGLE recipe or persistent profile provenance. Raw environment configuration remains possible. | Trainer profiling/compile capability. | Potentially better collective overlap. Auto PGLE does not operate on a directly called precompiled executable; Dew returns the jit callable, so it is a candidate, not a verified integration. | Unmeasured. |
| PGLE-aware persistent cache | Load a profile-optimized executable from the persistent cache. | [JAX PGLE cache workflow](https://docs.jax.dev/en/latest/gpu_performance_tips.html#collecting-nvidia-nsight-systems-profiles-when-using-autopgle) | JAX >=0.5.1 for JAX_COMPILATION_CACHE_EXPECT_PGLE; installed 0.11.1 | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Persistent cache exists; no PGLE cache workflow. | Trainer profiling/runtime capability. | Allows a later Nsight Systems run without simultaneous JAX profiling. Profile matching and cache invalidation must remain explicit. | Unmeasured. |
| tokamax serialized autotuning | Persist kernel tuning results and replay their chosen configurations. | [tokamax release](https://pypi.org/project/tokamax/0.0.13/); [tokamax usage guide](https://github.com/openxla/tokamax/blob/main/docs/basic_usage.md) | tokamax 0.0.13; Python >=3.12, JAX >=0.11.0 | Dropless sorted routing and XLA/tokamax grouped matmul (`src/dew/nn/moe.py:236-443`). | No tuning artifact in the run record. | Kernel configuration at attention_impl/MoE seams. | Reduces session-to-session configuration drift. Tuning choices may change numerical rounding; pin a selected result before parity claims. | Unmeasured. |

## Grain input features

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| Weighted named corpus mixtures | Mix records using stable named inputs and specified proportions. | [Grain IterDataset.mix; installed 0.2.18 lines 938-975](https://github.com/google/grain/blob/main/grain/_src/python/dataset/dataset.py#L938-L975) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | No general weighted LM corpus mixer. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Named iterator mixtures can restore components by key. They stop when any input exhausts; repeat individual training sources deliberately. | Unmeasured. |
| Best-fit packing | Select the tightest available packing bin. | [Grain BestFitPackIterDataset](https://google-grain.readthedocs.io/en/latest/grain.experimental.html#grain.experimental.BestFitPackIterDataset) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | Only first-fit document packing exists. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | May improve fill rate; changes record grouping and token order. Compare useful tokens per second and the exact loss mask. | Unmeasured. |
| Concat-then-split packing | Concatenate examples then cut fixed token blocks with explicit BOS handling. | [Grain ConcatThenSplitIterDataset](https://google-grain.readthedocs.io/en/latest/grain.experimental.html#grain.experimental.ConcatThenSplitIterDataset) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | No alternative packer selection. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Potentially simpler steady-state packing. Document boundaries and target transitions differ from first-fit semantics. | Unmeasured. |
| Worker autotuning | Choose a multiprocessing configuration under a host RAM budget. | [Grain pick_performance_config](https://google-grain.readthedocs.io/en/latest/grain.experimental.html#grain.experimental.pick_performance_config); [MaxText consumer](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/input_pipeline/data_processing_utils.py#L143-L150) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | Loading values are static; no RAM-budget tuner. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Can reduce loader starvation or excessive worker memory. Startup measurement is workload-specific and not a guarantee for later samples. | Unmeasured. |
| Parquet/TFRecord iterable sources and window shuffling | Read sequential shards with bounded-memory interleaving/shuffling. | [Grain experimental exports, installed experimental.py:37-69](https://google-grain.readthedocs.io/en/latest/grain.experimental.html) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | HF Arrow and token memmap paths exist; no generic LM sequential-shard adapter. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Can feed larger-than-local corpora. File-level worker partitioning and resumed read positions need evidence. | Unmeasured. |
| Pipeline stage timing | Measure per-transform time through DatasetOptions execution tracking. | [Grain DatasetOptions and ExecutionTrackingMode](https://google-grain.readthedocs.io/en/latest/grain.experimental.html) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | Dew measures loader throughput externally; no stage timing in the run record. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Identify decoding, mapping or packing stalls. Timing overhead and enabled mode must be recorded. | Unmeasured. |
| ElasticIterator global position | Checkpoint one global next-record index across changing shard counts. | [Grain ElasticIterator](https://google-grain.readthedocs.io/en/latest/grain.experimental.html#grain.experimental.ElasticIterator); [MaxText global-state save](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/input_pipeline/multihost_dataloading.py#L234-L238) | Grain 0.2.18 API/source verified; older floor not established | First-fit packed token documents with segment IDs (`src/dew/data/tokens.py:135-216`). Worker, thread and buffer controls (`src/dew/data/dataset.py:29-43`). | Dew checkpoints separate per-process positions and rejects changed process counts. | Data source/transform boundary; elastic state also touches Trainer Checkpoints. | Useful for portable restart on a different allocation. Requires unbatched/unsharded MapDataset input; variable-ratio filters and generic packing do not fit the current contract. | Unmeasured. |

## Orbax checkpoint features

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| Async saves and explicit completion | Move array saves off the training critical path and surface background errors at completion. | [Orbax async guide](https://orbax.readthedocs.io/en/latest/guides/checkpoint/async_checkpointing.html) | Orbax Checkpoint 0.12.4 source verified | Async persistent Orbax manager (`src/dew/checkpoints/__init__.py:121-150`). | Already enabled. The missing feature is scale/failure measurement, not an async boolean. | Retain Trainer Checkpoints and wait/close lifecycle. | Checkpoint copies still consume host/device bandwidth and host memory. Never equate asynchronous API return with a durable checkpoint. | Unmeasured. Dew source checks async completion; no new checkpoint benchmark ran. |
| OCDBT/Zarr3 and I/O budgets | Set array storage driver, target data-file size and save/restore concurrency. | [Orbax storage guide](https://orbax.readthedocs.io/en/latest/guides/checkpoint/optimized_checkpointing.html); [Installed handler source: lines 501-554,1147-1160](https://github.com/google/orbax/blob/main/checkpoint/orbax/checkpoint/_src/handlers/pytree_checkpoint_handler.py) | Orbax Checkpoint 0.12.4 verified; target OCDBT file size requires Zarr3 | Async persistent Orbax manager (`src/dew/checkpoints/__init__.py:121-150`). Per-process local copies; all hosts must retain the same step (`src/dew/checkpoints/__init__.py:159-207`). | Persistent handler defaults to OCDBT/Zarr2 in 0.12.4; local handler explicitly uses OCDBT/Zarr3. No I/O budget or file-size setting. | Trainer Checkpoints capability. | Can bound I/O concurrency and improve large-array storage behavior. Concurrent-byte limits do not cap total process RSS. | Unmeasured. |
| Partial restore and target shardings | Restore only requested leaves directly to declared target placements. | [Orbax PyTree restore guide](https://orbax.readthedocs.io/en/latest/guides/checkpoint/checkpointing_pytrees.html) | Orbax Checkpoint 0.12.4 source verified | Sharded partial restore; a global data position resumes on any process count and a shard offset needs the count that wrote it (`src/dew/checkpoints/__init__.py:106-138`). | Already passes partial_restore and target shardings. A changed data-process count is refused only for a shard offset. | Retain Checkpoints; solve iterator semantics separately. | No new storage feature needed. Resharded parameter restore does not establish elastic full-run recovery. | Unmeasured. |
| Restore transformations | Rename/map checkpoint leaves with transform specifications. | [Orbax transformations deprecation notice](https://orbax.readthedocs.io/en/latest/guides/checkpoint/transformations.html) | Orbax Checkpoint 0.12.4 retains transform arguments; current docs deprecate the library | Sharded partial restore; a global data position resumes on any process count and a shard offset needs the count that wrote it (`src/dew/checkpoints/__init__.py:106-138`). | Dew does not use deprecated transformations; it already uses partial restore. | Does not fit as a new core dependency; explicit interop conversion at DecoderFamily/checkpoint import. | Deprecated upstream. Prefer a named, validated conversion for a real layout change. | Unmeasured. |
| Replica-assisted local recovery | Recover from a surviving replica and broadcast local checkpoint state. | [Orbax emergency guide](https://github.com/google/orbax/tree/main/checkpoint/orbax/checkpoint/experimental/emergency); [MaxText multi-tier/emergency controls](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/base.yml#L475-L515) | Orbax Checkpoint 0.12.4 experimental emergency APIs; topology restrictions apply | Per-process local copies; all hosts must retain the same step (`src/dew/checkpoints/__init__.py:159-207`). Sharded partial restore; a global data position resumes on any process count and a shard offset needs the count that wrote it (`src/dew/checkpoints/__init__.py:106-138`). | All Dew hosts must retain the same local step; it cannot restore lost shards from another replica. | Trainer Checkpoints capability, with explicit replica topology. | Less lost work after a host/slice failure. Surviving-replica availability, data position and topology are required; faster local writes alone do not supply this. | Unmeasured. |

## Flax execution model

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| NNX native modules and graph transforms | Store variables on a mutable module graph and transform it with nnx.jit/grad/scan. | [Flax Linen-to-NNX guide](https://flax.readthedocs.io/en/latest/migrating/linen_to_nnx.html); [MaxText NNX defaults](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/base.yml#L1306-L1309) | Flax 0.12.9 API verified | Linen init/apply variables and raw JAX differentiation (`src/dew/training/trainer.py:224-238,291-299`). | No NNX object graph. A rewrite would change model construction, RNG handling, mutable collections and metadata. | Does not fit a blanket migration. Keep Linen for the current Objective and checkpoint contracts. | NNX can simplify imperative composition and upstream model reuse. There is no evidence here of a Dew throughput gain. | Unmeasured. |
| NNX functional split/merge | Separate NNX graph structure from parameter/state pytrees. | [Flax NNX functional API](https://flax.readthedocs.io/en/latest/nnx_basics.html#the-flax-functional-api) | Flax 0.12.9 API verified | Pure TrainState already separates state from model definition (`src/dew/training/trainer.py:224-238`). | Dew has no GraphDef or NNX State conversion. | Interop boundary for a named upstream NNX consumer. | Could reuse NNX code without making Trainer imperative. Tree paths, variable types, optimizer grouping and shared references need explicit conversion. | Unmeasured. |
| Linen/NNX bridge | Wrap Linen modules in NNX and NNX modules in Linen. | [Official nnx.bridge guide](https://flax.readthedocs.io/en/latest/guides/bridge_guide.html) | Flax 0.12.9 API verified; earlier bridge floor not established | DecoderFamily handles tensor/config translation only (`src/dew/interop/hf_decoders.py:2280-2302`). | No module-bridge adapter or round-trip graph/state guarantee. | DecoderFamily/Objective interop only when an upstream trainer requires it. | A bridge reduces porting scope. It does not automatically preserve cache collections, optimizer states, parameter names or sharding metadata. | Unmeasured. |

## GPU flags shipped in MaxText 0.2.4

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| `--xla_gpu_enable_latency_hiding_scheduler=true` | Schedule compute around asynchronous communication. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Can hide collectives if the graph has independent work; needs multi-device measurements. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_highest_priority_async_stream=true` | Use the highest-priority CUDA stream for async work. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Scheduling preference only; does not prove overlap or a speedup. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_all_reduce_combine_threshold_bytes=1073741824` | Combine small all-reduces up to a 1 GiB threshold in the 16-node recipe. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Fewer launches can delay readiness and reduce overlap. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_all_gather_combine_threshold_bytes=134217728` | Combine all-gathers using a 128 MiB threshold. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Larger buffers can improve link utilization and increase peak memory. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_reduce_scatter_combine_threshold_bytes=134217728` | Combine reduce-scatters using a 128 MiB threshold. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Threshold must fit gradient sizes and interconnect behavior. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_pipeline_all_gather=on` | Pipeline loop all-gathers. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | May overlap weight fetch with neighboring layer work; depends on loop structure. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_pipeline_reduce_scatter=on` | Pipeline loop reduce-scatters. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | May overlap gradient communication; can extend buffer lifetimes. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_pipeline_all_reduce=on` | Pipeline loop all-reduces. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | May overlap gradient reductions; verify memory and ordering. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_while_loop_double_buffering=true` | Double-buffer eligible loop inputs/outputs. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Can expose overlap but increases live buffers. Dew scan structure must be benchmarked. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_all_gather_combine_by_dim=false` | Allow all-gather combination across different gather dimensions. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | May combine more operations; layout and memory costs can change. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_reduce_scatter_combine_by_dim=false` | Allow reduce-scatter combination across different dimensions. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Same tradeoff for backward collectives. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_command_buffer=` | Disable CUDA command buffers in this MaxText recipe. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Do not copy as a Dew default. Dew recorded slower steps when command buffers were disabled. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_gpu_enable_triton_gemm=false` | Disable the Triton GEMM emitter in this recipe. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Different from disabling Pallas Triton. Kernel selection depends on shape and GPU generation. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |
| `--xla_disable_hlo_passes=rematerialization` | Disable the compiler HLO rematerialization pass. | [MaxText 0.2.4 GPU recipe](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/configs/gpu/a3/llama_2_7b/16vm.sh#L21-L30); [JAX GPU tuning guide](https://docs.jax.dev/en/latest/gpu_performance_tips.html) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | Raw flag injection exists; no tuned multi-device Dew preset. | Pre-backend Trainer runtime capability; keep hardware-specific presets out of model definitions. | Distinct from JAX checkpoint policies. Can increase HBM without a manually planned remat strategy. | GPU benefit unmeasured. Flag parser accepted on CPU with jaxlib 0.11.1; docs/performance.md:209-253 covers only a single GPU. |

## Related GPU flags not set by the shipped recipes

| Feature | What it does | Who ships it and source | Required/checked version | Dew today and application site | Gap | Fit | Expected win or risk | Evidence |
|---|---|---|---|---|---|---|---|---|
| `--xla_gpu_multi_streamed_windowed_einsum=true` | Overlap tensor-parallel GEMM chunks with their collectives. | [NVIDIA XLA collective-matmul and async guidance](https://github.com/NVIDIA/JAX-Toolbox/blob/main/docs/GPU_performance.md) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | No occurrences found in the six shipped configs/gpu shell recipes. Dew can already pass the raw flag. | Trainer runtime benchmark configuration. | A potential communication/matmul overlap path; graph patterns and link speed determine benefit. | GPU benefit unmeasured. CPU flag parser accepted it under jaxlib 0.11.1. |
| `--xla_gpu_threshold_for_windowed_einsum_mib=0` | Apply eligible collective-matmul rewriting without a minimum GEMM output-size threshold. | [NVIDIA XLA collective-matmul and async guidance](https://github.com/NVIDIA/JAX-Toolbox/blob/main/docs/GPU_performance.md) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | No occurrences found in the six shipped configs/gpu shell recipes. Dew can already pass the raw flag. | Trainer runtime benchmark configuration. | Zero can over-fragment small GEMMs. Tune for actual tensor-parallel shapes. | GPU benefit unmeasured. CPU flag parser accepted it under jaxlib 0.11.1. |
| `--xla_gpu_disable_async_collectives=allreduce` | Disable async execution for selected collective kinds. | [NVIDIA XLA collective-matmul and async guidance](https://github.com/NVIDIA/JAX-Toolbox/blob/main/docs/GPU_performance.md) | jaxlib 0.11.1 parser verified; flag introduction floor not established | Pre-backend XLA flags, cache and distributed setup (`src/dew/training/runtime.py:27-79`). | No occurrences found in the six shipped configs/gpu shell recipes. Dew can already pass the raw flag. | Trainer runtime benchmark configuration. | A diagnostic override. Disabling overlap is not an optimization by itself. | GPU benefit unmeasured. CPU flag parser accepted it under jaxlib 0.11.1. |

## Flag experiment and interpretation

The six shell recipes shipped under MaxText 0.2.4 `configs/gpu/` set the first flag table's scheduling, pipelining and threshold controls. The 1-, 2-, 4-, 8- and 16-node Llama 2 recipes use different reduction thresholds. The 128-node Llama 3.1 recipe has another configuration. The rows above quote the 16-node recipe; they are not suggested universal values.

Neither collective-matmul flag in the final table occurs in those six recipes. NVIDIA's JAX-Toolbox documents both. Its `disable_async_collectives` override is also separate from MaxText's highest-priority async stream and pipelining flags. Calling all of these “the MaxText async flags” would hide the difference between a source example and a candidate experiment.

All 17 flag assignments in the two tables were accepted by the installed jaxlib 0.11.1 parser. Each ran in its own process with `JAX_PLATFORMS=cpu`, a single `jax.devices()` call and core dumps disabled. Each printed `cpu`. Reproduce any row by substituting its exact assignment:

```sh
ulimit -c 0
JAX_PLATFORMS=cpu XLA_FLAGS='--xla_gpu_pipeline_all_gather=on' \
  /home/mrwhite0racle/Desktop/dew/.venv/bin/python -c 'import jax; print(jax.devices()[0].platform)'
```

This checks spelling and parser availability. It does not exercise GPU lowering, collectives, kernels or performance. The note does not recommend enabling the flags without a target-topology run. Command-buffer disabling is particularly unsuitable as a copied default: Dew already measured slower single-card execution with it (`docs/performance.md:219-247`).

## What dew needs

1. Decoder remat with named residuals. Begin with a full-remat baseline, then named saves and offload where HBM measurements justify them. MaxText's recipes use JAX policy constructors; they do not require NNX. Sources: [MaxText named policies](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/nnx_decoders.py#L1140-L1268) and [JAX remat](https://docs.jax.dev/en/latest/301/remat.html). Dew has only the DiT helper (`src/dew/nn/dit.py:263-284`).
   Fit: Trainer execution capability and Linen decoder block boundaries. Check saved values and backward memory on the real layer graph.
2. State placement by memory kind and role. Offload moments independently from weights, and establish persistent stage-local state before extending GPipe scheduling. Sources: [JAX memory spaces](https://docs.jax.dev/en/latest/201/memory-spaces.html) and [MaxText optimizer transfer](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/trainers/pre_train/train.py#L562-L632). Dew currently applies one device-memory layout rule mechanism to the whole state (`src/dew/training/distributed.py:205-259`; `src/dew/training/trainer.py:392-408`).
   Fit: Trainer placement capability and MeshSpec/Layout. Preserve donation and checkpoint restore contracts across memory spaces.
3. Device-qualified attention and expert kernels. Add Splash and useful tokamax paths, correct the hardware-specific cuDNN head limit, and make expert kernel selection reachable from decoder construction. Sources: [JAX cuDNN checks](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/_src/cudnn/fused_attention_stablehlo.py#L354-L402), [Splash blocks](https://github.com/jax-ml/jax/blob/jax-v0.11.1/jax/experimental/pallas/ops/tpu/splash_attention/splash_attention_kernel.py#L488-L548) and [tokamax release](https://pypi.org/project/tokamax/0.0.13/). Dew sites are `src/dew/nn/attention.py:445-453,559-662` and `src/dew/nn/moe.py:270-281`.
   Fit: attention_impl and MoE execution configuration. Require output, gradient, mask and cache parity before speed claims.
4. Topology-aware per-device communication. Extend the sequence and expert boundaries with kernels that do not require full K/V or token replication. Sources: [JAX shard_map](https://docs.jax.dev/en/latest/_autosummary/jax.shard_map.html) and [MaxText per-device MoE](https://github.com/AI-Hypercomputer/maxtext/blob/538fe7a3f3376d94cf3f04e77741aa6d7e8efa45/src/maxtext/layers/moe.py#L2306-L2360). Dew already uses shard_map for stage transfers (`src/dew/nn/backbones/causal_transformer.py:1569-1581`).
   Fit: MeshSpec/Layout and device-kernel seams. Keep the global Auto-sharded Trainer until an Explicit-mode experiment proves a benefit.
5. Scanned-layer execution measurements at realistic width. Evaluate modest unroll and remat_scan only after accounting for stack/unstack copies and optimizer leaf count. Sources: [Linen remat_scan](https://github.com/google/flax/blob/v0.12.9/flax/linen/transforms.py#L1087-L1149) and the existing Dew measurements (`docs/concepts/distributed.md:109-118`).
   Fit: decoder execution capability. Preserve the published parameter tree or plan an explicit cutover; do not hide a representation change inside a performance option.
6. Resumable weighted corpus input and bounded checkpoint I/O. Use Grain named mixtures and its measurement APIs. Evaluate ElasticIterator only for a pipeline its restrictions allow. Bound Orbax transfers and file chunks for the largest intended state. Sources: [Grain experimental APIs](https://google-grain.readthedocs.io/en/latest/grain.experimental.html) and [Orbax optimized checkpointing](https://orbax.readthedocs.io/en/latest/guides/checkpoint/optimized_checkpointing.html). Dew has fixed Loading values and per-process iterator snapshots (`src/dew/data/dataset.py:29-43`; `src/dew/checkpoints/__init__.py:339-353`).
   Fit: data source/transform seam and Trainer Checkpoints. A resumable corpus position must survive any supported resource change.
7. A versioned multi-device benchmark and profile record. Use the existing compile/trace seams to record collective overlap, HBM peak and sustained useful-token throughput. Include the exact jaxlib version and any PGLE/autotuning artifact. Sources: [JAX GPU tuning](https://docs.jax.dev/en/latest/gpu_performance_tips.html) and [NVIDIA XLA guidance](https://github.com/NVIDIA/JAX-Toolbox/blob/main/docs/GPU_performance.md). Dew's current measurements are single-card (`docs/benchmarks.md:21-28,52-66`).
   Fit: Trainer profiling/Tracker capabilities and benchmark tools. No new universal XLA flag default follows from this research.

Keep Linen. Use NNX split/merge or bridge conversion only for a named upstream consumer. The [Flax bridge guide](https://flax.readthedocs.io/en/latest/guides/bridge_guide.html) documents different RNG splitting and variable representations. Dew's current pure-variable Objective/Trainer interface is already suitable for the JAX features above (`src/dew/training/trainer.py:224-238,274-349`).

## Open questions for the owner

1. Which accelerator generation and host/interconnect topology should define the first performance acceptance run?
2. Should optional kernel integrations require Python 3.12 while core Dew retains its advertised Python 3.11 support? Tokamax 0.0.13 cannot satisfy a Python 3.11 extra.
3. Is reduced-precision attention an explicitly supported training mode, or should the kernel work stay within the existing exact-optimization contract? The unscaled FP8 Q/K experiments in MaxText cannot be treated as transparent optimizations.
