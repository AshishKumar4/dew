---
// The landing page's hero on `/`; Starlight's own hero everywhere else, such as the 404 page.
import Default from '@astrojs/starlight/components/Hero.astro';
import LandingHero from '../landing/LandingHero.astro';
---

{Astro.url.pathname === '/' ? <LandingHero /> : <Default />}
